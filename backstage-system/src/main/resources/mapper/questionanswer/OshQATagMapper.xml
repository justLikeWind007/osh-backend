<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backstage.system.mapper.questionanswer.OshQATagMapper">

    <select id="selectTagIdsByQuestionId" resultType="java.lang.Long">
        select tag_id from osh_question_answer_question_tag where question_id = #{questionId}
    </select>
</mapper>