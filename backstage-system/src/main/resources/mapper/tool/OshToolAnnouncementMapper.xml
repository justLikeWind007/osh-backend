<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backstage.system.mapper.tool.OshToolAnnouncementMapper">

    <select id="selectLatestToolAnnouncementsByChannel" resultType="com.backstage.system.domain.vo.tool.ToolAnnouncementVO">
        select id,
               title,
               link,
               create_time as createTime
        from osh_announcement
        where delete_flag = 0
          and status = 0
          and resource_type = 'tool'
          and channel = #{channel}
        order by create_time desc
        limit 5
    </select>

    <insert id="insertToolAnnouncement">
        insert into osh_announcement (
            title,
            link,
            resource_type,
            channel,
            status,
            delete_flag,
            create_by,
            create_time,
            update_by,
            update_time
        ) values (
            #{title},
            #{link},
            'tool',
            #{channel},
            0,
            0,
            #{operator},
            now(),
            #{operator},
            now()
        )
    </insert>

</mapper>
