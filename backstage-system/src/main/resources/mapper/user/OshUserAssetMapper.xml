<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backstage.system.mapper.user.OshUserAssetMapper">

    <update id="updatePointsAtomic">
        update osh_user_asset
        set points = points + #{delta},
            update_time = now(),
            update_by = #{userId}
        where user_id = #{userId}
          and delete_flag = 0
        <if test="requireEnough">
          and points + #{delta} &gt;= 0
        </if>
    </update>

</mapper>
