<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backstage.system.mapper.user.UserMapper">

    <resultMap type="User" id="UserResult">
        <result property="id"     column="id"      />
        <result property="username"     column="username"      />
        <result property="nickname"     column="nickname"      />
        <result property="email"     column="email"      />
        <result property="phone"     column="phone"      />
        <result property="password"     column="password"      />
        <result property="avatar"     column="avatar"      />
        <result property="weixinUnionid"     column="weixin_unionid"      />
        <result property="sex"     column="sex"      />
        <result property="desc"     column="desc"      />
        <result property="status"     column="status"      />
        <result property="createdTime"     column="created_time"      />
        <result property="updatedTime"     column="updated_time"      />
    </resultMap>

    <sql id="selectUser">
        select id, username, nickname, email, phone, password, avatar, weixin_unionid, sex, `desc`, `status`, created_time, updated_time from osh_user
    </sql>

    <select id="getUserByUsername" resultMap="UserResult">
        <include refid="selectUser"/>
        where status = 1 and username = #{username}
    </select>

    <insert id="register">
        insert into osh_user (username, password, status, created_time, updated_time) values (#{username},#{password},1,now(),now());
    </insert>



    <update id="updatePasswordById" >
        update osh_user set password = #{password} where id = #{userId}
    </update>

    <update id="updateUserInfoById" >
        update osh_user set avatar = #{avatar}, nickname = #{nickname}, sex = #{sex} where id = #{userId}
    </update>

    <select id="getPasswordById" resultType="java.lang.String">
        select password from osh_user where id = #{userId}
    </select>
    <select id="getUserInfoById" resultMap="UserResult">
        <include refid="selectUser"/>
        where status = 1 and id = #{userId}
    </select>
</mapper>